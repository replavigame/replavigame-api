spring:
  application:
    name: game-service
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
  #SWAGGER
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
